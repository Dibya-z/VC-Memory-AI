/**
 * Renders a DealAnalysis (Feature 3): summary, positive signals, risks, and
 * founder questions. The matched historical deals render via <SimilarDeals />.
 *
 * TODO(impl): accept a DealAnalysis prop and lay out the sections.
 */
export function AnalysisReport() {
  return (
    <div className="rounded-lg border bg-card p-6">
      {/* TODO: summary / signals / risks / questions */}
    </div>
  );
}
