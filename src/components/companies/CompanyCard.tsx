/**
 * Company summary card for the Company Memory grid: name, sector, stage, a
 * decision badge, and a one-liner. Links to the company detail page.
 *
 * TODO(impl): accept a Company prop and render fields + decision color badge.
 * Sibling components to add: CompanyFilters, CompanyTable.
 */
export function CompanyCard() {
  return (
    <div className="rounded-lg border bg-card p-5">
      {/* TODO: company name, sector/stage, decision badge, one-liner */}
    </div>
  );
}
