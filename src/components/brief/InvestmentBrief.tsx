/**
 * Renders an InvestmentBrief (Feature 4): the standard partner-ready memo
 * sections, ending with a hedged recommendation ("Needs partner review" /
 * "Interesting signal" / "Needs more diligence") — never a final decision.
 *
 * TODO(impl): accept an InvestmentBrief prop; render sections + print/export.
 */
export function InvestmentBrief() {
  return (
    <div className="rounded-lg border bg-card p-6">
      {/* TODO: Overview, Problem, Solution, Market, Founder Signals, Traction,
          Strengths, Risks, Similar Past Deals, Questions, Recommendation */}
    </div>
  );
}
